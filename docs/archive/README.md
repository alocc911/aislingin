# Archive

This folder holds historical material that should not distract from daily project guidance.

- `debug/`: screenshots, dated JSON dumps, and one-off debug artifacts.
- `raw-notes/`: binary Word documents and old raw notes that need human review before being converted to durable Markdown.

When information here becomes active project guidance, extract the useful parts into a normal Markdown file under `docs/`.
